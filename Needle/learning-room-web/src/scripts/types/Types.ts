export type QustionsType = {
    de: [
        {
            question: string,
            answer: string
        }
    ], 
    en: [
        {
            question: string,
            answer: string
        }
    ]
}